numpy>=1.24.0
Pillow>=10.0.0
pillow-heif>=0.13.0
opencv-python>=4.8.0
pyyaml>=6.0
# Install the CUDA 12.6 build of torch/torchvision (GPU support). On Windows the
# default PyPI wheels are CPU-only, so install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
albumentations>=1.4.0
pandas>=2.0.0
scikit-learn>=1.3.0
scikit-image>=0.22.0
pytest>=7.0.0
